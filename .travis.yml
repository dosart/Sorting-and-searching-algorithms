language: python

python:
  - "3.5"
  - "3.6"
  - "3.7"
  - "3.8"

install:
  - pip install poetry
  - pip install wemake-python-styleguide
  - pip install pytest
  - pip install pytest-cov
  - pip install coverage
  - make install

script:
  - make check
